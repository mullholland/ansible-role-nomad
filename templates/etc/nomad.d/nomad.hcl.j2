# {{ ansible_managed }}

{% for section in nomad_config_sections %}
# ---------------------------------------------------------------------------
# {{ section.name }}
# ---------------------------------------------------------------------------
{{ section.config }}
{% endfor %}
